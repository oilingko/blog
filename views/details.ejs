<%- include('./partials/header.ejs') %>

<div class="post-detail-content">
    <% if (post) { %>
        <h2><%= post.title %></h2>
        <div class="post-meta">
            <span><i class="fa-solid fa-user"></i> <%= post.author %></span>
            <span><i class="fa-solid fa-calendar-days"></i> <%= post.createdAt.toLocaleDateString('bn-BD') %></span>
        </div>
        <hr>
        <% if (post.thumbnailUrl) { %>
            <img src="<%= post.thumbnailUrl %>" alt="<%= post.title %>">
        <% } %>
        <p><%- post.content %></p>
    <% } else { %>
        <p>দুঃখিত, পোস্টটি খুঁজে পাওয়া যায়নি।</p>
    <% } %>
    <br>
    <a href="/">&laquo; হোমপেজে ফিরে যান</a>
</div>

<%- include('./partials/footer.ejs') %>
